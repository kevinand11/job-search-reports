# Job Search Reports

Private repository for generated job-search HTML reports.

## Structure

- `index.html` — landing page for report links.
- `reports/` — timestamped HTML reports generated by the job-search workflow.

## Workflow

The scheduled search should create personalized CVs as Google Docs, generate an HTML report that links each role to its personalized CV, and commit that report into `reports/`.
